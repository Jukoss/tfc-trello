overwrite: true
schema: http://localhost:8080/v1/graphql
documents: src/app/core/graphql/*.gql
generates:
  src/app/core/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
